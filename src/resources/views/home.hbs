<div class="container mt-4">
  <div class="row">
    {{#each courses}}
      <div class="col-md-6">
        <div class="card">
          <img src="{{this.image}}" class="card-img-top" alt="image-banner">
          <div class="card-body">
            <h5 class="card-title">{{this.name}}</h5>
            <p class="card-text">{{this.description}}</p>
            <a href="/khoa-hoc/{{this.slug}}" class="btn btn-primary">Xem thêm</a>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>